
const uiReducer = (state = {}, action) => {
  // switch(action.type){
  // case():
  //   return Object.assign({}, action);
  // default:
  //   return state;
  // }
  return state;
};

export default uiReducer;
